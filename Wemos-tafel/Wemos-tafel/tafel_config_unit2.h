#ifndef TAFEL_CONFIG_H
#define TAFEL_CONFIG_H


#define TAFEL_CONFIG(n) Tafel(D1, D2, 2, n) 

// Voorbeeld met meerdere tafels:
// #define TAFEL_CONFIG(n) Tafel(D1, D2, 2, n), Tafel(D5, D6, 3, n)

#endif
